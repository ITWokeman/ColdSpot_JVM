
零、基本使用
为了提升本jvm的运行效率，建议使用目录下jdk1.8文件夹里面的simple-rt.jar作为jdk
此jdk为jdk1.8版rt.jar的的简化版本，删除了大量不常用的.class文件，保留了java程序中常用的.class文件
此可极大提升本jvm检索.class文件的速度。
当然，如果配置路径正确，本jvm也可读取其他jdk，但是执行速度可能会因检索.class文件而有所降低

一、替代功能
由于本虚拟机不完善，部分功能无法通过jdk的类库直接使用
为了不影响相关功能的使用，本虚拟机自带类库作为补充。
详见sflib
